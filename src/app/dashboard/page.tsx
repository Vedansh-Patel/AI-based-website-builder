'use client'
import dynamic from 'next/dynamic'

export default function DashboardPage() {
  return (
    <div className="p-6">
      <h2 className="text-2xl font-semibold">Canvas (placeholder)</h2>
      <p className="mt-2">This area will host the drag & drop canvas and component palette.</p>
    </div>
  )
}
